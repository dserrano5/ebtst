<h2><%=l 'Number of notes per year' %></h2>
<% foreach my $elem (@$nby) { %>
    <b><%= $elem->{'year'} %></b> - <%= $elem->{'count'} %> (<%= $elem->{'pct'} %>%) - 
    <% foreach my $d (@{ $elem->{'detail'} }) { %>
        [<%= $d->{'count'} %> <img src="images/<%= $d->{'value'} %>.gif">]
    <% } %>
    <br>
<% } %>
